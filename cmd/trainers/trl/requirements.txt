# trl 1.9.2 forwards unrecognised flags to `accelerate launch`; older releases
# split arguments differently, so re-check before bumping.
trl==1.9.2
transformers>=4.57.0
accelerate>=1.10.0
peft>=0.17.0
datasets>=3.0.0
